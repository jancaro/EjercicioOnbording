import { ValidatorPasswordMatchingDirective } from './validator-password-matching.directive';

describe('ValidatorPasswordMatchingDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidatorPasswordMatchingDirective();
    expect(directive).toBeTruthy();
  });
});
